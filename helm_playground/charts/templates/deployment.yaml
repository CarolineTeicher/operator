apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: helloworld
spec:
  selector:
    matchLabels:
      app: helloworld
  replicas: 1
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      containers:
      - name: helloworld
        image: "{{ .Values.image }}:{{ .Values.tag }}"
        ports:
        - containerPort: {{ .Values.port }}