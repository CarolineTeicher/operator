apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  chart:
    repository: https://CarolineTeicher.github.io/operator/helm_playground
    name: podinfo
    version: 3.2.0
  values:
    replicaCount: 2